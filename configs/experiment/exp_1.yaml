# @package _global_
defaults:
  - override /model: squeezenet
  - override /optimizer: rms

# Global variables:
#epochs: 1
#batch_size: 24

# Set variables for specific sub-config files (e.g. rms.yaml):
#optimizer:
#  lr: 1e-3
# model:
#  transfer_learning_model_path: models/model.pth
